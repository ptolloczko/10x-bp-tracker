---
// src/pages/measurements.astro
import Layout from "@/layouts/Layout.astro";
import MeasurementsView from "@/components/views/MeasurementsView";
import { isFeatureEnabled } from "@/features/flags";

// Check if measurement feature is enabled
if (!isFeatureEnabled("measurement")) {
  return Astro.redirect("/");
}

// Check if user is authenticated
// If not, redirect to login page
if (!Astro.locals.user) {
  return Astro.redirect("/login");
}
---

<Layout title="Lista Pomiarów">
  <MeasurementsView client:load />
</Layout>
