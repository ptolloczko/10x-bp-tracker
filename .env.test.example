# Environment variables for E2E tests
# Copy this file to .env.test and fill in the values

# Application URL
BASE_URL=http://localhost:3000

# Supabase Configuration for Testing
# Use your test/staging environment or local Supabase
PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (for database cleanup in tests)
# WARNING: This key bypasses Row Level Security - use only in test environment!
# Get this from: npx supabase status (look for "service_role key")
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Instructions:
# 1. Start local Supabase: npx supabase start
# 2. Copy this file: cp .env.test.example .env.test
# 3. Get credentials from: npx supabase status
# 4. Fill in .env.test:
#    - PUBLIC_SUPABASE_ANON_KEY (anon key from supabase status)
#    - SUPABASE_SERVICE_ROLE_KEY (service_role key from supabase status)
# 5. Run tests: npm run test:e2e
#
# Note: Tests will create dynamic users (test-{timestamp}@example.com)
#       which will be automatically cleaned up after all tests complete
